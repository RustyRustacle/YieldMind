{
  "name": "yieldmind",
  "version": "1.0.0",
  "description": "AI-Powered Yield Intelligence Protocol on Polkadot Hub",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network polkadot-hub-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0",
    "@openzeppelin/contracts": "^5.4.0",
    "ethers": "^6.16.0"
  }
}
